import Layout from '../../containers/Layout';
import { LoginComponent, Footer } from '../../components';

export default function Login() {
  return (
    <Layout>
      <LoginComponent />
    </Layout>
  );
}
