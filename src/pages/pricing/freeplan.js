import Layout from '../../containers/Layout';
import { FreeplanRegistration } from '../../components';

export default function Freeplan() {
  return (
    <Layout>
      <FreeplanRegistration />
    </Layout>
  );
}
