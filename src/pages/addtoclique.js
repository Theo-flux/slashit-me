import Layout from '../containers/Layout';
import { JoinThisClique } from '../components';

export default function AddToClique() {
  return (
    <Layout>
      <JoinThisClique />
    </Layout>
  );
}
