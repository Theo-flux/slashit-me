import React from 'react';
import { PayForm, PaySection } from './payStyles';

function Pay() {
  return (
    <PayForm>
      <PaySection>Payment form</PaySection>
    </PayForm>
  );
}

export default Pay;
